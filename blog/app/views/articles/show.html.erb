
<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<!--Estas dos lineas muestran los comentarios de un articulo, llamando a una pagina partial en la carpeta de comments-->
<h2>Comments</h2>
<%= render @article.comments %>

<!--Esta seccion es para agregar nuevos comentarios por medio de una pagina parcial de comments-->
<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
