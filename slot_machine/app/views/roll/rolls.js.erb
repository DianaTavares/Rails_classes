// primero indicamos el selector en el cual esta la vista parcial que se refresca al seleccionar el sibmit. le indicamos que es un archivo html y que al presionarlo refresque solo ese parcial.
$('#die').html('<%= j render('die_roll') %>');
